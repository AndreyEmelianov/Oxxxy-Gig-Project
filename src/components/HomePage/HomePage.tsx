import { FC } from 'react';

import { MainBanner } from '../MainBanner/MainBanner';

export const HomePage: FC = () => {
  return (
    <main className="main">
      <MainBanner />
    </main>
  );
};
